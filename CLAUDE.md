@AGENTS.md

# CafeCanvas — Claude session

Read `AGENTS.md` for the full 9-agent team, global rules, and orchestration protocol.

Custom subagents live in `.cursor/agents/` (and `.claude/agents/` if mirrored). Delegate by name, e.g. "Use db-agent to add the blogs migration."
