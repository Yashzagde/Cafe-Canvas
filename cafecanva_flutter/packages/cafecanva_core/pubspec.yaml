name: cafecanva_core
description: CafeCanva core engine — models, auth, Supabase client, repositories
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  flutter_secure_storage: ^9.2.2
  bcrypt: ^1.1.3
  connectivity_plus: ^6.0.3     # Connectivity detection support
  flutter_riverpod: ^2.5.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
